import React from "react";

export const Footer = () => {
  return (
    <div className="footer">
      Left to do: troubleshoot exit animations, code in locking to component on
      gallery launch, mobile friendly version
    </div>
  );
};
